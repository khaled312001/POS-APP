# Superseded builds

Pre-rebrand artifacts from the Barmagly era, kept only for reference. They point
at `pos.barmagly.tech` and carry the old branding — **do not upload them**.

The current uploads are one directory up:
`kassenta-pos-v10.aab` and `customer-app/play-store/kassenta-order-v4.aab`.
