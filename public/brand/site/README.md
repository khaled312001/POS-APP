# Website images

Drop the generated images here using the exact slot id as the file name, e.g.
`hero-pos-tablet.webp`. Prompts and sizes: `docs/KASSENTA_IMAGE_PROMPTS.md`.

Until a file exists the page renders a neutral placeholder with the slot id on it,
so a partial set never breaks the layout.
