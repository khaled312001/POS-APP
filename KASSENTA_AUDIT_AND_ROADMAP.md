# Kassenta POS System — تقرير الفحص الفني وخطة التطوير

> **الحالة:** مسودة تنفيذية · **التاريخ:** 9 أغسطس 2026 · **النطاق:** المنظومة كاملة (POS · العميل · الدليفري · السوبر أدمن)
> **الأساس:** فحص مباشر للكود المصدري — كل بند موثّق بالملف ورقم السطر. الأرقام مقيسة، مش مقدّرة.

---

## المحتويات

1. [الملخّص التنفيذي](#1-الملخّص-التنفيذي)
2. [منهجية الفحص وما تم قياسه](#2-منهجية-الفحص-وما-تم-قياسه)
3. [خريطة النظام الحالي](#3-خريطة-النظام-الحالي)
4. [نتائج الفحص — الأخطاء](#4-نتائج-الفحص--الأخطاء)
   - [4.1 حرجة](#41-حرجة--تمنع-الإطلاق-التجاري)
   - [4.2 عالية](#42-عالية--تؤثر-على-التشغيل-اليومي)
   - [4.3 متوسطة](#43-متوسطة--دين-تقني)
5. [الربط بين الأنظمة الأربعة](#5-الربط-بين-الأنظمة-الأربعة)
6. [خطة ترقية التصميم والوايت مود](#6-خطة-ترقية-التصميم-والوايت-مود)
7. [معمارية الموديولات حسب نوع النشاط](#7-معمارية-الموديولات-حسب-نوع-النشاط)
8. [القابلية للتوسع والبنية التحتية](#8-القابلية-للتوسع-والبنية-التحتية)
9. [متطلبات السوق الأوروبي](#9-متطلبات-السوق-الأوروبي)
10. [خارطة الطريق التنفيذية](#10-خارطة-الطريق-التنفيذية)
11. [الأسبوع الأول — مهام فورية](#11-الأسبوع-الأول--مهام-فورية)
12. [ملحق: مقاييس خط الأساس](#12-ملحق-مقاييس-خط-الأساس)

---

## 1. الملخّص التنفيذي

النظام **غنيّ وظيفيًا بشكل يفوق منتجات كتير في نفس الفئة**: متعدد المستأجرين، أربع تطبيقات مترابطة، تعريف متصل (Caller ID)، تكامل واتساب، منظومة دليفري كاملة بسائقين ومناطق وتتبّع، ونسخ احتياطي لكل مستأجر.

ومع ذلك، **النظام في وضعه الحالي غير جاهز للإطلاق التجاري في أوروبا** لثلاثة أسباب جوهرية — وكلها قابلة للإصلاح:

| # | السبب | الأثر |
|---|---|---|
| 1 | **الأمان** — صفر تحقق صلاحيات على السيرفر. كل قيود الأدوار في المتصفح فقط، وبيانات الموظف مخزّنة بدون توقيع | أي كاشير يقدر يبقى مالك ويسحب قاعدة الـ39 ألف عميل |
| 2 | **الضرائب** — معدل ضريبة واحد لكل فرع، والمعدل الافتراضي (7.7%) ملغي من يناير 2024 | كل فاتورة فيها أصناف مختلطة بتطلع غلط قانونيًا |
| 3 | **الأوفلاين** — غير موجود إطلاقًا | الكاشير بيقف مع أول قطع نت — أول سؤال بيسأله أي عميل أوروبي |

### توزيع النتائج

| التصنيف | العدد | المعنى |
|---|---|---|
| 🔴 حرجة | 5 | توقف الإطلاق التجاري |
| 🟠 عالية | 8 | تؤثر على التشغيل اليومي أو الأمان |
| 🟡 متوسطة | 7 | دين تقني بيبطّئ كل تطوير جاي |

### ما هو سليم بالفعل ✅

- **عزل المستأجرين** على مستوى SQL شغال بشكل صحيح، مع `onDelete: cascade` على كل الجداول التابعة.
- **لوحة السوبر أدمن محمية بالكامل** — 68 مسار من 69 عليهم `requireSuperAdmin`، والمسار الوحيد المكشوف هو تسجيل الدخول نفسه.
- **جدول التشغيلات `product_batches` موجود فعليًا** — الأساس للصيدليات جاهز.
- **تطبيق السائق (PWA)** أنضج مكوّن في المنظومة.
- **بنية بيانات ناضجة** — 66 جدول تغطي الورديات والمرتجعات والجرد وأوامر الشراء والولاء.
- **العربية RTL مدعومة** بالفعل عبر `language-context`.

---

## 2. منهجية الفحص وما تم قياسه

| ما تم فحصه | الطريقة |
|---|---|
| سلامة الأنواع | `npx tsc --noEmit` على المشروع كامل |
| تحقق الصلاحيات | بحث شامل في `server/routes.ts` عن أي فحص دور — النتيجة صفر |
| حماية السوبر أدمن | عدّ المسارات مقابل المسارات المحمية بـ`requireSuperAdmin` |
| منطق الفلوس | قراءة كاملة لـ`lib/cart-context.tsx` + كل مواضع `parseFloat`/`toFixed` في السيرفر |
| نموذج البيانات | قراءة `shared/schema.ts` (66 جدول) للضرائب والوزن والتشغيلات |
| الأوفلاين والـPWA | بحث عن `NetInfo` / service worker / طوابير محلية |
| التدويل والعملات | عدّ مواضع `CHF` الثابتة وفحص مجموعات مفاتيح الترجمة |
| التصميم | قراءة `constants/colors.ts` وطريقة استهلاك الألوان في الشاشات |

---

## 3. خريطة النظام الحالي

### المكوّنات

```
Kassenta/
├── app/                    تطبيق الكاشير — Expo Router (Web → Android/iOS)
│   ├── (tabs)/index.tsx        شاشة البيع            4,631 سطر ⚠️
│   ├── (tabs)/settings.tsx     الإعدادات             3,402 سطر ⚠️
│   ├── (tabs)/reports.tsx      التقارير              1,973 سطر
│   ├── (tabs)/online-orders.tsx الطلبات الأونلاين    1,828 سطر
│   └── … عملاء · منتجات · QR الطاولات · مناطق التوصيل · السائقين · الكوبونات
│
├── server/                 Express + Drizzle ORM (MySQL)
│   ├── routes.ts               مسارات المستأجرين     4,791 سطر ⚠️
│   ├── storage.ts              طبقة الوصول للبيانات   2,453 سطر
│   ├── superAdminRoutes.ts     69 مسار إدارة المنصة   1,203 سطر
│   ├── whatsappService · callerIdService · deliveryService · broadcastRoutes
│   └── templates/              5 صفحات HTML (متجر · لوحات · هبوط)  12,059 سطر
│
├── customer-app/           تطبيق العميل — WebView فقط ⚠️
├── delivery-app/           PWA السائق + تتبّع + بث الطلبات
└── shared/schema.ts        66 جدول
```

### الحقائق التقنية

| البند | الواقع | ملاحظة |
|---|---|---|
| قاعدة البيانات | **MySQL** (`mysqlTable`) | التوثيق في `replit.md` بيقول PostgreSQL — **غير صحيح** |
| العملة الافتراضية | CHF | مثبتة نصيًا في 128 موضع |
| اللغات | EN · AR · DE | **ناقص FR · IT** (لغتان رسميتان في سويسرا) |
| المصادقة | مفتاح ترخيص + PIN | PIN نص صريح · مفيش توكن موظف |
| الأوفلاين | ❌ غير موجود | ولا service worker للـPOS |
| الاختبارات | ❌ صفر | مفيش أي ملف اختبار |

---

## 4. نتائج الفحص — الأخطاء

### تصنيف سريع

| المعرّف | العنوان | الخطورة | الجهد |
|---|---|---|---|
| SEC-01 | مفيش تحقق صلاحيات على السيرفر | 🔴 حرج | أسبوعان |
| SEC-02 | أكواد PIN نص صريح | 🔴 حرج | 3 أيام |
| SEC-03 | سر السييد مكتوب في الكود + مسار تدمير بيانات | 🔴 حرج | 30 دقيقة |
| TAX-01 | ضريبة واحدة للفرع + معدل ملغي | 🔴 حرج | أسبوعان |
| BIZ-01 | رسوم 20 فرنك مثبتة على كل الطلبات | 🔴 حرج | ساعة |
| OPS-01 | مفيش وضع أوفلاين | 🟠 عالية | 3 أسابيع |
| FIN-01 | حساب فلوس بـfloat + مفيش تقريب 0.05 | 🟠 عالية | أسبوع |
| BUG-01 | تصادم معرّفات أصناف السلة | 🟠 عالية | ساعتان |
| BUG-02 | شحن المحفظة Stripe مكسور | 🟠 عالية | دقيقتان |
| QLT-01 | 130 خطأ TypeScript | 🟠 عالية | أسبوع |
| I18N-01 | 128 موضع CHF مثبت | 🟠 عالية | 3 أيام |
| SEC-04 | مسارات عامة بمطابقة البادئة | ✅ **منفّذ** | — |
| SEC-05 | WebView مفتوح على أي دومين | 🟠 عالية | 15 دقيقة |
| OPS-02 | مفيش طباعة مطبخ/ESC-POS | 🟠 عالية | أسبوعان |
| DEBT-01…05 | ملفات ضخمة · صفر اختبارات · ترجمة يدوية · توثيق قديم · RTL | 🟡 متوسطة | مستمر |

---

### 4.1 حرجة — تمنع الإطلاق التجاري

#### 🔴 SEC-01 — مفيش أي تحقق صلاحيات على السيرفر

**الموقع:** `server/routes.ts` (كامل الملف) · `lib/auth-context.tsx` · `app/(tabs)/index.tsx:1145`

البحث في 4,791 سطر عن أي فحص لدور الموظف رجّع **صفر نتيجة**. الاعتماد الوحيد للـAPI هو هيدر `x-license-key` — وهو مفتاح **واحد لكل المحل** بيدي صلاحية كاملة على كل شيء.

كل صلاحيات الأدوار موجودة في المتصفح فقط:

```ts
// lib/auth-context.tsx — كلها تُحسب على العميل
const isAdmin = role === "admin" || role === "owner";
const canAccessReports = isAdmin || isManager || perms.includes("reports");
// وبيانات الموظف تُخزَّن بدون توقيع:
AsyncStorage.setItem("barmagly_employee", JSON.stringify(emp));
```

**سيناريو الاستغلال (على الويب — وهي النسخة الأساسية):**

```js
// من Console المتصفح، بحساب كاشير عادي:
localStorage.barmagly_employee = JSON.stringify({ ...emp, role: "owner" });
location.reload();   // ← الآن مالك، بكل الشاشات والصلاحيات
```

وحتى بدون ذلك، مفتاح الترخيص نفسه موجود في `localStorage` ويمكن استخدامه لاستدعاء الـAPI مباشرة: سحب قاعدة العملاء (39,000+ سجل)، تعديل الأسعار، حذف مبيعات، وتجاوز سقف خصم الكاشير المحسوب في المتصفح فقط:

```ts
// app/(tabs)/index.tsx:1145 — قيد صوري
const maxFixed = isCashier ? cart.subtotal * (maxCashierDiscountPct / 100) : Infinity;
```

**الإصلاح:**

```ts
// 1) عند دخول الموظف بالـPIN → أصدر توكن
const token = jwt.sign(
  { tenantId, employeeId: emp.id, role: emp.role, branchId: emp.branchId },
  JWT_SECRET,
  { expiresIn: "12h" }          // ينتهي مع نهاية الوردية
);

// 2) ميدلوير الصلاحيات
export const requireRole = (...roles: Role[]) =>
  (req: AuthedRequest, res: Response, next: NextFunction) => {
    if (!req.employee) return res.status(401).json({ error: "Employee token required" });
    if (!roles.includes(req.employee.role)) return res.status(403).json({ error: "Forbidden" });
    next();
  };

// 3) على كل مسار كتابة/تقرير
app.delete("/api/products/:id", requireRole("owner", "admin", "manager"), …);
app.get("/api/reports/*",       requireRole("owner", "admin", "manager"), …);
app.post("/api/sales",          requireRole("owner", "admin", "manager", "cashier"), …);
```

- سقف الخصم يُعاد التحقق منه على السيرفر باستخدام `req.employee.role`.
- الترخيص يبقى لتفعيل الجهاز فقط — الصلاحيات من توكن الموظف.

**معيار الإنجاز:** مفيش مسار كتابة أو تقرير واحد بدون `requireRole`، ومحاولة استدعاء مسار أدمن بتوكن كاشير بترجع 403.

---

#### 🔴 SEC-02 — أكواد PIN مخزّنة نصًا صريحًا

**الموقع:** `shared/schema.ts:30` · `server/routes.ts:839`

```ts
// shared/schema.ts:30
pin: text("pin").notNull(),          // ← بدون تشفير

// server/routes.ts:839
if (!emp || emp.pin !== req.body.pin) { … }   // ← مقارنة نصية مباشرة
```

أي تسريب لنسخة احتياطية = كل أكواد دخول كل الموظفين في كل المحلات مكشوفة. ومخالفة مباشرة لالتزامات GDPR / nDSG بخصوص تأمين بيانات الاعتماد.

**الإصلاح:**

```ts
// السكيمة
pinHash: text("pin_hash").notNull(),

// التحقق
const ok = await argon2.verify(emp.pinHash, req.body.pin);

// الهجرة: عند أول دخول ناجح بالقيمة القديمة → خزّن الهاش واحذف العمود القديم
```

بالإضافة إلى **تحديد المحاولات** (rate limit) — PIN من 4 أرقام يعني 10,000 احتمال فقط، وبدون حد يتم تخمينه في دقائق.

---

#### 🔴 SEC-03 — سر السييد مكتوب في الكود + مسار يمسح الكتالوج

**الموقع:** `server/tenantAuth.ts:106-112`

```ts
const seedSecret = process.env.SEED_SECRET || "barmagly-seed-2026-zrh";  // ← قيمة احتياطية معروفة
if (req.headers["x-seed-secret"] === seedSecret) return next();
```

أي شخص اطّلع على الكود يقدر يستدعي مسارات السييد على الإنتاج. والأخطر: `seedPizzaLemon()` في وضع الإنتاج بتعمل **إعادة ضبط كاملة للكتالوج** (حذف + إعادة إدخال) — يعني ده مسار تدمير بيانات محمي بسر معروف.

**الإصلاح:**
- حذف القيمة الاحتياطية نهائيًا.
- تعطيل مسارات السييد في الإنتاج **بشكل قاطع** (مش بسر):
  ```ts
  if (isSeedRoute(req.path) && process.env.NODE_ENV === "production") {
    return res.status(404).end();
  }
  ```
- نقل بيانات العرض التوضيحي لسكريبت CLI منفصل يُنفَّذ يدويًا خارج السيرفر.

---

#### 🔴 TAX-01 — معدل ضريبة واحد للفرع + معدل افتراضي ملغي

**الموقع:** `shared/schema.ts:18` · `lib/cart-context.tsx:58,131`

الضريبة الوحيدة في السكيمة كلها:

```ts
// shared/schema.ts:18 — على الفرع
taxRate: decimal("tax_rate", { precision: 5, scale: 2 }).default("0"),

// المنتج عنده بوليان فقط
taxable: boolean("taxable").default(true),
```

يعني **كل الأصناف بتتحسب بنفس النسبة**. والواقع الأوروبي:

| الدولة | الأكل والمشروبات غير الكحولية | باقي الأصناف | ملاحظة |
|---|---|---|---|
| 🇨🇭 سويسرا | **2.6%** | **8.1%** | الأكل داخل المحل (dine-in) = 8.1% · نفس الصنف takeaway = 2.6% |
| 🇩🇪 ألمانيا | 7% | 19% | نفس منطق dine-in / takeaway |
| 🇦🇹 النمسا | 10% | 20% | |

وفوق ده، الافتراضي في الكود:

```ts
// lib/cart-context.tsx:58
const [taxRate, setTaxRate] = useState(7.7);   // ← معدل انتهى العمل به 31 ديسمبر 2023
```

والحساب على مستوى الفاتورة كلها مش السطر:

```ts
// lib/cart-context.tsx:131
const tax = ((subtotal - discount + minimumOrderSurcharge) * taxRate) / 100;
```

**نقطة إضافية حرجة:** في أوروبا الأسعار المعروضة للمستهلك يجب أن تكون **شاملة الضريبة** (Bruttopreis). النظام حاليًا بيضيف الضريبة *فوق* السعر — لازم يدعم الوضعين بإعداد لكل مستأجر.

**الإصلاح:**

```sql
CREATE TABLE tax_classes (
  id INT PRIMARY KEY AUTO_INCREMENT,
  tenant_id INT NOT NULL,
  name VARCHAR(64) NOT NULL,          -- "أكل 2.6%" · "قياسي 8.1%"
  rate DECIMAL(5,2) NOT NULL,
  rate_dine_in DECIMAL(5,2) NULL,     -- تبديل حسب نوع الطلب
  is_default BOOLEAN DEFAULT FALSE
);
ALTER TABLE products   ADD COLUMN tax_class_id INT NULL;
ALTER TABLE categories ADD COLUMN tax_class_id INT NULL;  -- وراثة للقسم
ALTER TABLE tenants    ADD COLUMN prices_include_tax BOOLEAN DEFAULT TRUE;
```

- حساب الضريبة **لكل سطر** ثم التجميع لكل فئة ضريبية.
- الإيصال يعرض تفصيل الضريبة لكل فئة (شرط قانوني).

---

#### 🔴 BIZ-01 — رسوم حد أدنى 20 فرنك مثبتة على كل الطلبات

**الموقع:** `lib/cart-context.tsx:121-133`

```ts
const MIN_ORDER_AMOUNT = 20;

const minimumOrderSurcharge = useMemo(
  () => (items.length > 0 && subtotal - discount < MIN_ORDER_AMOUNT
        ? MIN_ORDER_AMOUNT - (subtotal - discount) : 0),
  [items.length, subtotal, discount]
);

const total = subtotal - discount + minimumOrderSurcharge + tax + deliveryFee + serviceFee;
```

السلة بتضيف الفرق تلقائيًا لأي طلب أقل من 20 — **من غير أي شرط على نوع الطلب ولا على المستأجر**:

- كافيه بيبيع قهوة بـ4.50 → العميل بيتحاسب **20.00**
- سوبرماركت بيبيع علبة لبن بـ2.00 → العميل بيتحاسب **20.00**
- حتى طلب dine-in داخل المطعم بيتحاسب نفس القاعدة

القاعدة مكتوبة أصلًا لحالة دليفري مطعم واحد وبقت قاعدة عامة للمنتج كله.

**الإصلاح:**

```sql
ALTER TABLE tenants ADD COLUMN min_order_amount DECIMAL(10,2) NULL;      -- NULL = معطّل
ALTER TABLE tenants ADD COLUMN min_order_applies_to JSON;                -- ["delivery"]
```

مع تعطيلها افتراضيًا لكل المستأجرين الجدد.

---

### 4.2 عالية — تؤثر على التشغيل اليومي

#### 🟠 OPS-01 — مفيش وضع أوفلاين إطلاقًا

البحث عن أي بنية أوفلاين (طابور محلي · `NetInfo` · service worker للـPOS) رجّع **لا شيء**. مجلد `public/` فيه `sounds` فقط — من غير `sw.js` ولا `manifest.json` للتطبيق (الـPWA الموجود يخص تطبيق الدليفري وحده).

كل عملية بيع بتروح للسيرفر مباشرة عبر `apiRequest` بمهلة 20 ثانية. النت يقطع → الكاشير يقف.

**المعمارية المقترحة:**

```
┌─ الكتالوج ─────────────────────────────────────────┐
│  IndexedDB (ويب) / SQLite (موبايل)                 │
│  مزامنة تفاضلية عند الاتصال (updated_at)           │
└───────────────────────────────────────────────────┘
┌─ البيع ────────────────────────────────────────────┐
│  كل عملية تُكتب محليًا أولًا بـ clientSaleId فريد    │
│  (UUID) ثم تدخل طابور الإرسال                      │
└───────────────────────────────────────────────────┘
┌─ المزامنة ─────────────────────────────────────────┐
│  POST /api/sales يقبل clientSaleId ويكون idempotent │
│  إعادة إرسال نفس المعرّف ترجع نفس النتيجة، مش نسخة  │
└───────────────────────────────────────────────────┘
```

**قواعد لازمة:**
- ترقيم الإيصالات أوفلاين بمقطع خاص بالجهاز (`T2-000145`) لتفادي التصادم.
- المخزون أوفلاين = تقديري، والتسوية النهائية عند المزامنة.
- مؤشر حالة دائم للكاشير: **متصل / يعمل أوفلاين / بيزامن (N عملية)**.

**معيار الإنجاز:** بيع ساعة كاملة بدون إنترنت ثم مزامنة بصفر فقد وصفر تكرار.

---

#### 🟠 FIN-01 — حسابات الفلوس بأرقام عائمة + مفيش تقريب 0.05

قاعدة البيانات مضبوطة (`decimal(10,2)`) لكن كل الحساب في جافاسكريبت:

```ts
const subtotal = items.reduce((sum, i) => sum + i.price * i.quantity, 0);
const discount = (subtotal * discountRate) / 100;
const tax = ((subtotal - discount + surcharge) * taxRate) / 100;
```

الفرق بيظهر كفرق قروش في التقارير وعند تسوية الدرج. وسويسرا بتقرّب النقدي لأقرب **5 راپن** — غير مطبّق (التقريب الوحيد الموجود في `server/routes.ts:905` هو لأقرب 0.5 داخل دالة تسعير عرض توضيحي).

**الإصلاح:**

```ts
// كل الحسابات بالسنتات (integer)
const subtotalCents = items.reduce((s, i) => s + i.priceCents * i.quantity, 0);

// تقريب نقدي قابل للضبط لكل عملة
export function roundCash(cents: number, increment = 5): number {  // CHF = 5
  return Math.round(cents / increment) * increment;
}
// ملاحظة: التقريب على الإجمالي النقدي فقط — البطاقة تُحصَّل بالقيمة الدقيقة
```

---

#### 🟠 BUG-01 — تصادم معرّفات أصناف السلة

**الموقع:** `lib/cart-context.tsx:70-92`

```ts
id: Date.now(),   // ← صنفان في نفس الملي ثانية = نفس المعرّف
```

صنفين بيتضافوا في نفس الملي ثانية (ضغط سريع أو قارئ باركود بيرسل دفعة) بياخدوا نفس المعرّف — وبعدين `updateQuantity` و`removeItem` بيأثروا على الاتنين مع بعض.

ومعاها مشكلة تانية: دمج الأصناف المكررة بيتم بمقارنة **نصوص الأسماء**:

```ts
const existing = prev.find((i) =>
  i.productId === product.id &&
  (variantName ? i.name.includes(variantName) : !i.name.includes("(")) &&
  JSON.stringify(i.modifiers || []) === JSON.stringify(product.modifiers || [])
);
```

أي منتج اسمه أصلًا فيه قوس — مثلًا `بيتزا (عائلي)` — بيكسر المنطق. وكمان `JSON.stringify` بيتأثر بترتيب الخصائص.

**الإصلاح:**

```ts
id: crypto.randomUUID(),
// ومفتاح دمج مبني على الهوية مش الاسم:
const mergeKey = `${productId}|${variantId ?? ""}|${hashModifiers(modifiers)}`;
```

---

#### 🟠 BUG-02 — شحن المحفظة عبر Stripe مكسور دائمًا

**الموقع:** `server/routes.ts:4322-4323`

```ts
const stripe = getUncachableStripeClient();      // ← بترجع Promise<Stripe> بدون await
const intent = await stripe.paymentIntents.create({ … });   // ← TypeError دائمًا
```

مؤكد من مدقق الأنواع: `Property 'paymentIntents' does not exist on type 'Promise<Stripe>'`. المسار بيرمي استثناء ويرجع 500 في كل مرة.

**الإصلاح:** `const stripe = await getUncachableStripeClient();` — ومراجعة باقي استدعاءات نفس الدالة.

---

#### 🟠 QLT-01 — 130 خطأ TypeScript والبناء ماشي فوقهم

```
$ npx tsc --noEmit | grep -c "error TS"
130
```

| الملف | العدد | ملاحظة |
|---|---|---|
| `app/` | 49 | |
| `server/routes.ts` | 33 | من ضمنها BUG-02 |
| `server/seedAllDemoData.ts` | 6 | استخدام `.returning()` وهي غير موجودة في درايفر MySQL — **كود بيرمي وقت التشغيل** |
| `lib/i18n.ts` | 6 | |
| `server/index.ts` | 5 | |
| `server/storage.ts` | 4 | `Property 'where' does not exist` — استعلامات مكسورة |

الأخطاء دي هي اللي كشفت BUG-02، وفيه على الأقل نظير له لسه مستور.

**الإصلاح:** تصفير الأخطاء على دفعات + `tsc --noEmit` كبوابة إلزامية في CI.

---

#### 🟠 I18N-01 — 128 موضع بكلمة CHF مثبتة

الفرع عنده حقل `currency` والواجهة بتخليك تختار العملة — لكن العرض والإيصالات والقوالب فيها `CHF` مكتوبة نصًا في **128 موضع**:

```tsx
<Text>CHF {cart.subtotal.toFixed(2)}</Text>
`TOTAL: CHF ${Number(inv.totalAmount).toFixed(2)}`
```

أي عميل في منطقة اليورو هيشوف أسعاره بالفرنك السويسري. وكمان مفيش تنسيق أرقام حسب اللغة: السويسري `1'234.50` مقابل الألماني `1.234,50` مقابل الفرنسي `1 234,50`.

**الإصلاح:**

```ts
export function formatMoney(cents: number, currency: string, locale: string) {
  return new Intl.NumberFormat(locale, { style: "currency", currency })
    .format(cents / 100);
}
// formatMoney(123450, "CHF", "de-CH") → "CHF 1'234.50"
// formatMoney(123450, "EUR", "de-DE") → "1.234,50 €"
```

---

#### ✅ SEC-04 — قائمة المسارات العامة بمطابقة البادئة *(منفّذ)*

**الموقع:** `server/tenantAuth.ts:72`

```ts
if (PUBLIC_ROUTES.some(route => path.startsWith(route))) return true;
```

50+ مدخل بمطابقة بادئة. مدخل زي `"/api/delivery/orders"` بيفتح **كل** المسارات اللي بتبدأ بيه، مش مسار إنشاء الطلب فقط. المساحة المكشوفة أوسع بكتير من المقصود، والتحقق الفعلي متروك لكل معالج على حدة.

**الإصلاح:** تطابق دقيق أو أنماط صريحة، وميدلوير مخصص لتوكن السائق/العميل بدل الاستثناء العام:

```ts
const PUBLIC_EXACT = new Set(["/api/health", "/api/license/validate", …]);
const PUBLIC_PATTERNS = [/^\/api\/store\/\d+\/menu$/, /^\/api\/delivery\/orders\/track\/[\w-]+$/];
```

---

#### 🟠 SEC-05 — تطبيق العميل WebView مفتوح على أي دومين

**الموقع:** `customer-app/app/index.tsx`

```tsx
const CUSTOMER_URL = "https://pos.barmagly.tech/customer/";   // ← دومين مثبت
<WebView originWhitelist={["https://*", "http://*"]} … />      // ← أي موقع
```

أي رابط داخل الصفحة يقدر ياخد المستخدم لأي موقع **جوه** التطبيق وبنفس ثقة العلامة — خطر تصيّد، وسبب شائع لرفض المراجعة في متاجر التطبيقات. والدومين مثبت في الكود ولازم يتغيّر مع إعادة التسمية لـKassenta.

**الإصلاح:**

```tsx
const CUSTOMER_URL = process.env.EXPO_PUBLIC_CUSTOMER_URL!;
<WebView
  originWhitelist={["https://kassenta.com", "https://*.kassenta.com"]}
  onShouldStartLoadWithRequest={(req) => {
    if (!req.url.startsWith("https://kassenta.com")) { Linking.openURL(req.url); return false; }
    return true;
  }}
/>
```

---

#### 🟠 OPS-02 — مفيش طباعة مطبخ ولا ESC/POS حقيقية

**الموقع:** `utils/printing.ts`

الطباعة عبر `expo-print` (شاشة طباعة النظام) على الموبايل، و`iframe` على الويب. مفيش:

- أوامر ESC/POS ولا اتصال مباشر بطابعة شبكة
- توجيه أصناف لطابعة المطبخ/البار حسب القسم
- شاشة مطبخ (KDS)
- فتح درج النقدية

لمطعم فيه مطبخ، ده مش تفصيلة — ده شرط تشغيل.

**الإصلاح:**

```ts
// خدمة طباعة ESC/POS عبر TCP 9100
await escpos.print({ host: printer.ip, port: 9100, commands });

// توجيه بالأقسام
printers: [
  { name: "المطبخ", ip: "192.168.1.50", categories: [1, 2, 5] },
  { name: "البار",   ip: "192.168.1.51", categories: [7] },
]
```

+ KDS كصفحة ويب على تابلت المطبخ تستقبل عبر WebSocket.

---

### 4.3 متوسطة — دين تقني

| المعرّف | المشكلة | التفصيل |
|---|---|---|
| **DEBT-01** | ملفات ضخمة | `app/(tabs)/index.tsx` = 4,631 سطر · `settings.tsx` = 3,402 · `server/routes.ts` = 4,791. أي تعديل بسيط بيلمس ملف عملاق، والتقسيم لموديولات شبه مستحيل قبل التفكيك |
| **DEBT-02** | صفر اختبارات | منظومة بتحسب فلوس وضرايب من غير ولا اختبار واحد على منطق السلة — أكبر مصدر خطر عند التوسع |
| **DEBT-03** | ترجمة يدوية | `lib/i18n.ts` = 2,383 سطر، ثلاث مجموعات مفاتيح متوازية مكتوبة يدوي بدون فحص تطابق. أي مفتاح ناقص بيظهر للمستخدم كنص خام. التوسع لست لغات بالطريقة دي غير عملي |
| **DEBT-04** | توثيق مضلل | `replit.md` بيقول PostgreSQL و`pgTable` بينما السكيمة `mysqlTable`، وبيقول "PWA مع service worker" وده غير موجود. أي مطور جديد هيتضلل من أول يوم |
| **DEBT-05** | RTL واللغات | `I18nManager.forceRTL` ما بيسري على الموبايل غير بعد إعادة تشغيل. واللغات EN/AR/DE فقط — **مفيش FR ولا IT** وهما رسميتان في سويسرا |
| **DEBT-06** | مفيش بيع بالوزن | `inventory.quantity` = `int` فقط، ومفيش `weighable` ولا `pricePerKg`. **يمنع نشاط السوبرماركت والجزارة والخضار تمامًا** |
| **DEBT-07** | مفيش مراقبة إنتاج | مفيش Sentry ولا سجل منظّم ولا تنبيهات. النشر بسكريبتات بايثون يدوية (`deploy_*.py`) |

---

## 5. الربط بين الأنظمة الأربعة

| الرابط | الحالة | الفجوة |
|---|---|---|
| **سوبر أدمن ← المستأجرين** | ✅ سليم | 69 مسار، 68 محمي بـ`requireSuperAdmin` والباقي هو الدخول. عزل `tenantId` على مستوى SQL شغال |
| **سوبر أدمن ← التراخيص** | 🟡 ناقص | الترخيص مفتاح واحد بصلاحية كاملة. لازم يتفصل: الترخيص للتفعيل، وتوكن الموظف للصلاحيات. ومفيش ربط بين حالة الاشتراك وقفل الموديولات |
| **POS ↔ الطلبات الأونلاين** | 🟡 جزئي | WebSocket شغال للتنبيه، لكن مفيش إعادة اتصال مضمونة ولا طابور — طلب بيوصل والتطبيق مقفول ممكن يضيع. محتاج polling احتياطي + إقرار استلام (ack) |
| **POS ↔ تطبيق العميل** | 🟠 ضعيف | WebView على دومين مثبت (SEC-05). مفيش push أصلي ولا أوفلاين ولا deep links. عمليًا هو المتصفح بأيقونة |
| **POS ↔ الدليفري/السائق** | ✅ جيد | PWA ببث الطلبات ومناطق التوصيل وتتبّع حي. أنضج جزء. ينقصه GPS مستمر وتقدير وقت وصول (ETA) |
| **المخزون بين القنوات** | 🟠 خطر | البيع من الكاشير والطلب الأونلاين بيخصموا من نفس المخزون **بدون قفل صفّي** — بيع متزامن على آخر قطعة = مخزون سالب |

**إصلاح المخزون المتزامن:**

```sql
START TRANSACTION;
SELECT quantity FROM inventory
  WHERE product_id = ? AND branch_id = ? FOR UPDATE;   -- ← القفل الناقص
UPDATE inventory SET quantity = quantity - ? WHERE …;
COMMIT;
```

---

## 6. خطة ترقية التصميم والوايت مود

### الوضع الحالي

`constants/colors.ts` = ملف واحد فيه ~60 لون ثابت **للوضع الداكن فقط**، مستخدمين مباشرة في كل شاشة (`Colors.surface`, `Colors.text`…). وحتى المفتاح المسمّى `light` في آخر الملف قيمه داكنة — بقايا قالب Expo:

```ts
export default {
  light: {
    text: "#FFFFFF",        // ← أبيض في وضع "فاتح"؟
    background: "#0A0E27",  // ← كحلي غامق
  },
};
```

**الخلاصة: الوايت مود مش سويتش — ده إعادة بناء لطبقة الألوان.**

### الخطوة 1 — نظام تصميم بالرموز (Design Tokens)

استبدال الألوان الثابتة برموز **دلالية** تصف الوظيفة لا اللون:

```
tokens/
├── palette.ts     القيم الخام:     teal.500 = #2FD3C6 · navy.900 = #0A0E27
├── semantic.ts    الدلالة:         bg.base · bg.raised · fg.primary · fg.muted · border.subtle
└── themes.ts      الثيمات:         dark · light · highContrast

ThemeProvider      system | light | dark + حفظ الاختيار لكل جهاز
useTheme()         بديل import { Colors }
```

| قبل | بعد |
|---|---|
| `Colors.background` | `theme.bg.base` |
| `Colors.surface` | `theme.bg.raised` |
| `Colors.text` | `theme.fg.primary` |
| `Colors.accent` | `theme.accent.default` |

### الخطوة 2 — وضع فاتح مصمَّم لا معكوس

- **التباين:** التركوازي `#2FD3C6` على أبيض تباينه **1.7:1** — أقل بكثير من حد WCAG AA (4.5:1). لازم درجة أغمق للوضع الفاتح: `#0C8F85` (تباين 4.6:1)، مع الاحتفاظ بالفاتح للوضع الداكن.
- **وضع ثالث عالي التباين** لشاشات الكاشير قرب الواجهات الزجاجية والإضاءة القوية — دي ميزة بيع حقيقية في السوق الأوروبي، مش رفاهية.
- احترام `prefers-color-scheme` على الويب و`Appearance` على الموبايل، مع تخطٍّ يدوي محفوظ.

### الخطوة 3 — تحسينات تجربة شاشة البيع

| التحسين | السبب |
|---|---|
| كل زر ≥ **48×48** نقطة | الكاشير بيضغط بإبهامه وهو واقف ومستعجل |
| تفكيك `index.tsx` لمكوّنات (شبكة الأصناف · السلة · الدفع · الإضافات) | 4,631 سطر مستحيل صيانتها |
| **اختصارات لوحة المفاتيح** على الويب (F2 دفع · F3 بحث · ESC إلغاء) | التاجر الأوروبي بيشتغل بلوحة مفاتيح وقارئ باركود |
| حالات فاضية وحالات خطأ حقيقية | بدل شاشة بيضا: "مفيش أصناف في القسم ده — أضف صنف" |
| **مؤشر حالة الاتصال** دائم | متصل / يعمل أوفلاين / بيزامن (N) — بعد OPS-01 |
| إتاحة الوصول: تباين AA · قارئ شاشة · خط قابل للتكبير | شرط في مناقصات القطاع العام الأوروبي |

---

## 7. معمارية الموديولات حسب نوع النشاط

### الوضع الحالي

الحقل موجود بالفعل:

```ts
// shared/schema.ts:535
storeType: text("store_type").default("supermarket"),  // supermarket, restaurant, pharmacy, others
```

لكنه **شكلي**: بيتخزّن ويتعرض في الإعدادات، ومفيش سلوك واحد في التطبيق بيتغيّر بناءً عليه.

### المبدأ: قلب ثابت + قدرات قابلة للتركيب

> ❌ **مش** نسخة تطبيق لكل مجال — ده كابوس صيانة.
> ✅ نواة واحدة (منتجات · بيع · مخزون · عملاء · تقارير · ورديات) + **قدرات (capabilities)** تُفعَّل حسب النشاط والباقة.

```sql
CREATE TABLE modules (
  key VARCHAR(64) PRIMARY KEY,        -- "kitchen.kds"
  name_ar TEXT, name_en TEXT, name_de TEXT,
  capabilities JSON,                   -- ["kitchen.printing", "kitchen.display"]
  min_plan VARCHAR(32)                 -- basic | pro | enterprise
);

CREATE TABLE tenant_modules (
  tenant_id INT, module_key VARCHAR(64),
  enabled BOOLEAN DEFAULT TRUE,
  settings JSON,
  PRIMARY KEY (tenant_id, module_key)
);

CREATE TABLE vertical_presets (
  vertical VARCHAR(32) PRIMARY KEY,    -- restaurant | cafe | supermarket | pharmacy | …
  modules JSON                          -- الحزمة الافتراضية عند التسجيل
);
```

```ts
// الواجهة
const hasKDS = useCapability("kitchen.display");
{hasKDS && <KitchenDisplayTab />}

// السيرفر — الأهم
app.post("/api/kds/orders", requireCapability("kitchen.display"), …);
```

> ⚠️ **الدرس المستفاد من SEC-01:** بوابات القدرات لازم تُفرض على **السيرفر** مش الواجهة. أي قيد في المتصفح فقط مش قيد.

### حزم الأنشطة

| النشاط | الموديولات المفعّلة | المطلوب بناؤه |
|---|---|---|
| **مطاعم** | الطاولات · مخطط الصالة · KDS · الأقسام (مطبخ/بار) · الكورسات · تقسيم الفاتورة · البقشيش · الدليفري | مخطط صالة مرئي · شاشة مطبخ · توجيه الطباعة بالأقسام · تقسيم حساب الطاولة · دمج/نقل الطاولات |
| **كافيهات** | بيع سريع · مفضّلات · ولاء · طلب مسبق · اشتراكات قهوة | شاشة "الأكثر مبيعًا" بضغطة · تعديلات سريعة (حجم/حليب/سكر) · كوب مجاني بعد N |
| **سوبرماركت** | باركود ووزن · ميزان · عروض الكميات · موردون · أوامر شراء · جرد | ⚠️ **البيع بالوزن (غير موجود — الكمية `int` فقط)** · باركود EAN-13 موزون · عروض 3×2 · استلام بضاعة |
| **صيدليات** | التشغيلات والصلاحية · الروشتات · البدائل · التأمين · المواد المراقبة | `product_batches` موجود ✅ — ينقصه بيع **FEFO** (الأقرب انتهاءً أولًا) · سجل الروشتة · تنبيه التعارض الدوائي |
| **مخابز** | إنتاج يومي · هالك · طلبات مسبقة · نصف تصنيع | خطة إنتاج الصباح · تسجيل الهالك آخر اليوم · طلبات المناسبات |
| **ملابس وتجزئة** | مصفوفة مقاس/لون · مرتجعات واستبدال · كروت هدايا · حجز قطعة | مصفوفة متغيّرات حقيقية (الحالي JSON بسيط) · سياسة استبدال · رصيد عميل |
| **خدمات وحجوزات** | مواعيد · مقدّم خدمة · عمولات · اشتراكات | تقويم حجز · ربط الخدمة بالموظف · احتساب عمولة تلقائي |

### تعديل السكيمة المطلوب للوزن (يفتح نشاط كامل)

```sql
ALTER TABLE products  ADD COLUMN is_weighable BOOLEAN DEFAULT FALSE;
ALTER TABLE products  ADD COLUMN price_per_unit DECIMAL(10,3);   -- سعر الكيلو
ALTER TABLE products  ADD COLUMN unit_type VARCHAR(16) DEFAULT 'piece';  -- piece|kg|g|l
ALTER TABLE inventory MODIFY quantity DECIMAL(12,3) DEFAULT 0;   -- ← int لا يكفي
ALTER TABLE sale_items MODIFY quantity DECIMAL(12,3) NOT NULL;
```

### سجل موديولات معلن (declarative)

كل موديول ملف واحد بيعلن شاشاته ومساراته وجداوله وصلاحياته — إضافة نشاط جديد تبقى **إضافة ملف**، مش تعديل في 20 شاشة:

```ts
// modules/kitchen/module.ts
export default defineModule({
  key: "kitchen",
  capabilities: ["kitchen.printing", "kitchen.display"],
  screens: [{ path: "/kds", component: KitchenDisplay, roles: ["admin", "manager", "chef"] }],
  routes: registerKitchenRoutes,
  tables: [kitchenTickets],
  verticals: ["restaurant", "cafe", "bakery"],
});
```

---

## 8. القابلية للتوسع والبنية التحتية

| المجال | المطلوب |
|---|---|
| **قاعدة البيانات** | فهارس مركّبة على `(tenant_id, created_at)` لكل جدول ضخم · تقسيم `sales` بالتاريخ قبل تجاوز بضعة ملايين صف · مراجعة خطط التنفيذ للاستعلامات البطيئة |
| **الطوابير** | نقل الواتساب والإيميل والفواتير والنسخ الاحتياطي لطابور خلفي (BullMQ) — حاليًا كلها داخل دورة الطلب وبتبطّئ الكاشير |
| **الكاش** | Redis للكتالوج وإعدادات المستأجر — أكثر البيانات قراءةً وأقلها تغيّرًا |
| **المراقبة** | Sentry للأخطاء · سجل منظّم بـ`tenantId` · تنبيه على معدل الفشل · مقاييس زمن الاستجابة. **حاليًا صفر رصد للإنتاج** |
| **CI/CD** | بوابة `tsc --noEmit` + اختبارات + بناء تلقائي. النشر حاليًا بسكريبتات بايثون يدوية (`deploy_*.py`) — مصدر خطر مباشر |
| **الاختبارات** | وحدة على منطق السلة والضرائب (الأولوية القصوى) · تكامل على مسارات البيع · E2E على رحلة بيع كاملة |
| **API عامة + Webhooks** | شرط للتكامل مع المحاسبة الأوروبية (bexio · Abacus · DATEV) ومنصات التوصيل |
| **النسخ الاحتياطي** | موجود ✅ — ينقصه **اختبار استعادة دوري** موثّق |

---

## 9. متطلبات السوق الأوروبي

> دي مش تحسينات — دي **شروط دخول قانونية**. تجاهل أي بند منها = غرامات أو منع بيع.

| الدولة | الاشتراط | الأثر على النظام |
|---|---|---|
| 🇨🇭 **سويسرا** | **لا يوجد إلزام بجهاز ضريبي** — ميزة كبيرة للانطلاق | المطلوب: VAT صحيح (8.1% / 2.6% / 3.8%) · أسعار شاملة الضريبة · تقريب 0.05 · **TWINT** (وسيلة الدفع الأولى محليًا — مذكورة في القوالب فقط، غير منفّذة) |
| 🇩🇪 **ألمانيا** | KassenSichV — وحدة أمان تقنية **TSE** إلزامية + إيصال إلزامي | تكامل TSE سحابي (Fiskaly / Deutsche Fiskal) · توقيع كل عملية · تصدير **DSFinV-K** |
| 🇦🇹 **النمسا** | RKSV — توقيع إلكتروني وسلسلة إيصالات مترابطة | بطاقة توقيع · QR على كل إيصال · تقرير شهري وسنوي |
| 🇫🇷 **فرنسا** | برنامج معتمد (NF525 أو شهادة مطوّر) | سجل غير قابل للتعديل · أرشفة · ترقيم متسلسل بدون فجوات |
| 🇮🇹 **إيطاليا** | إرسال يومي للسلطة الضريبية | ربط بطابعة RT أو خدمة إرسال معتمدة |

**مشترك — حماية البيانات:** GDPR (الاتحاد الأوروبي) و nDSG (سويسرا). وعندك **39,000+ سجل عميل فعلي**، وتعريف متصل، وتكامل واتساب. المطلوب:

- [ ] تصدير وحذف بيانات العميل بضغطة (حق النقل وحق النسيان)
- [ ] سياسة احتفاظ محددة المدة
- [ ] سجل أنشطة المعالجة
- [ ] اتفاقية معالجة بيانات (DPA) مع كل تاجر — أنت **معالج** وهو **مراقب**
- [ ] موافقة صريحة قبل أي رسالة تسويقية على واتساب

### توصية استراتيجية

**ابدأ بسويسرا.** هي السوق الوحيدة اللي مش محتاجة اعتماد ضريبي — تقدر تبيع فيها خلال 3 شهور. ألمانيا والنمسا محتاجين تكامل TSE/RKSV وده مشروع لوحده (6–10 أسابيع + اشتراك سنوي لكل جهاز). ادخلهم في مرحلة تانية بعد ما يبقى عندك عملاء وإيراد.

**وبخصوص السوق العربي:** مفيش اشتراط ضريبي مماثل، لكن الأولويات بتنقلب:

- الأوفلاين بيبقى **أهم** ميزة على الإطلاق (شبكة أقل استقرارًا)
- تعدد العملات ضروري من اليوم الأول
- العربي RTL موجود بالفعل ✅
- الفاتورة الإلكترونية إلزامية في مصر والسعودية لو هتدخلهم

---

## 10. خارطة الطريق التنفيذية

> **الترتيب مقصود:** مفيش أي شغل واجهة أو موديولات قبل ما الأساس يبقى آمن وصحيح ماليًا. بناء موديولات فوق نظام بلا صلاحيات = مضاعفة المشكلة.

### المرحلة 0 · الهوية والانتقال للدومين الجديد — منفّذة

**الهوية البصرية.** اللوجو الرسمي `logo.jpeg` بقى مصدر الحقيقة الوحيد. سكريبت
[`scripts/generate-brand-assets.py`](scripts/generate-brand-assets.py) بيولّد **كل** الأصول منه آليًا —
مفيش أي أيقونة اتعملت بالإيد، وإعادة تشغيل السكريبت آمنة في أي وقت:

| المخرج | الاستخدام |
|---|---|
| `assets/images/icon.png` (1024) | أيقونة التطبيق |
| `assets/images/android-icon-{foreground,background,monochrome}.png` | الأيقونة التكيّفية (أندرويد) |
| `assets/images/splash-icon.png` | شاشة البداية |
| `assets/images/play-store-{icon,feature-graphic}.png` | متجر جوجل |
| `assets/images/favicon.png` + `public/brand/favicon-{16,32,48,180,192,512}.png` + `favicon.ico` | المتصفح |
| `assets/brand/logo-{full,mark}{,-white,-teal,-dark-bg}.png` | الموقع والقوالب والمستندات |

**ألوان الهوية** مأخوذة بالعيّنة من اللوجو نفسه: **Navy `#040E32`** · **Teal `#00C1B0`** —
مطبّقة في `constants/colors.ts` و`app.json` وقوالب السيرفر.

> **ملاحظة تصميمية:** اللوجو الأصلي مرسوم لخلفية فاتحة، فنصفه الكحلي كان بيختفي على الخلفية
> الكحلية للأيقونة. السكريبت بيولّد نسخة `for_dark_bg` بتحوّل الكحلي لأبيض وتحتفظ بالتركوازي.

**الدومين.** `pos.barmagly.tech` → `kassenta.com` (100 استبدال في 22 ملف).

| البند | القرار |
|---|---|
| قاعدة البيانات | `u492425110_kassenta` — منقولة بالكامل (65 جدول · 40,191 عميل · 11 مستأجر · 13 ترخيص) |
| مساحة التطبيق | `~/kassenta-app` **جديدة ومنفصلة** — الموقع القديم شغال لحد ما الجديد يتأكد 100% |
| النطاقان | `kassenta.com` و `backend.kassenta.com` على **نفس `PassengerAppRoot`** → عملية Node واحدة، مفيش migrations ولا seeding مرتين، ومفيش CORS |
| مفاتيح `AsyncStorage` | **بدون تغيير** (`barmagly_license_key` …) — تغييرها معناه خروج كل جهاز مفعّل وفقدان تفعيله |
| معرّف الحزمة (`tech.barmagly.pos`) | **بدون تغيير** — تغييره على جوجل بلاي = تطبيق جديد بالكامل وفقدان التقييمات والتنزيلات. الاسم الظاهر بس هو اللي اتغيّر لـ Kassenta POS |
| تراخيص `BARMAGLY-…` القديمة | شغّالة — التحقق بحث في قاعدة البيانات مش فحص بادئة |

**قائمة التحقق قبل حذف الدومين القديم** — كلها لازم تعدّي على `kassenta.com`:

- [ ] `/api/health` يرجّع `{ok:true, database:"mysql"}`
- [ ] الصفحة الرئيسية (Landing) بتفتح باللوجو الجديد
- [ ] `/super_admin/login` → دخول ناجح → لوحة المنصة بتعرض المستأجرين الـ11
- [ ] `/app` → شاشة الترخيص → تفعيل بترخيص حقيقي
- [ ] دخول موظف بالـPIN + إصدار `x-employee-token`
- [ ] شاشة البيع: تحميل المنتجات (322) والأقسام (62)
- [ ] عملية بيع كاملة + إيصال
- [ ] `/dashboard` لصاحب المحل
- [ ] `/store/:tenantId` واجهة المتجر العامة
- [ ] الطلبات الأونلاين + إشعار WebSocket
- [ ] الصور والرفوعات (`/uploads`) بتظهر

### المرحلة 0.5 · الثيم والموقع والتقسية — منفّذة (11 أغسطس 2026)

#### أ) طبقة الثيم (وايت مود افتراضي)

النظام كله بقى بيقرأ من **مجموعة توكنات واحدة** بدل ألوان ثابتة:

| الملف | الدور |
|---|---|
| [`constants/theme.ts`](constants/theme.ts) | لوحتان (`lightPalette` / `darkPalette`) بنفس المفاتيح بالضبط + الرَنتايم اللي بيحدد اللوحة النشطة |
| [`constants/colors.ts`](constants/colors.ts) | `Colors` بقى **Proxy** بيقرأ اللوحة النشطة — كل الـ2,395 استخدام قديم اشتغل من غير أي تعديل |
| [`lib/themed-styles.ts`](lib/themed-styles.ts) | `themedStyles()` بديل لـ`StyleSheet.create` عند مستوى الموديول، بيعيد الحساب مرة واحدة بس عند تبديل الثيم |
| [`lib/theme-context.tsx`](lib/theme-context.tsx) | `ThemeProvider` + `useTheme()` + الحفظ في `kassenta_theme` |

**قرار معماري:** التبديل بيتم بإعادة رندر مش بإعادة تركيب (remount) — `ThemeProvider ← LanguageProvider ← كل الشاشات`.
كل شاشة بتستدعي `useLanguage()` أصلًا، فالتبديل بيوصل للتطبيق كله في خطوة واحدة **من غير ما يضيع مكان التنقّل
أو محتوى السلة**.

**قيد مهم على اللوحة:** كل قيمة في اللوحة عبارة عن **hex بستة خانات**. السبب إن ~200 موضع في الكود
بيبني ألوان شفافة بدمج نصوص (`Colors.accent + "22"`) — لو الأساس كان `rgba(...)` الناتج بيبقى لون غير صالح.

**التباين:** كل لون نص/أيقونة بيعدّي WCAG AA (4.5:1). الأكسنت في الوضع الفاتح هو `#0C8F85` مش
`#00C1B0` (اللي تباينه 1.9:1 على الأبيض فقط).

- زر التبديل موجود في: هيدر كل تاب، شاشة الكاشير الرئيسية، الإعدادات (سويتش مجزّأ)، الموقع، السوبر أدمن، لوحة المتجر.
- **الافتراضي وايت مود + إنجليزي** في التطبيق والموقع والسوبر أدمن ولوحة المتجر.

#### ب) الموقع — من صفحة واحدة إلى موقع متكامل

[`server/site/`](server/site/) — قشرة واحدة بتولّد **9 صفحات**:
`/` · `/features/` · `/solutions/` · `/pricing/` · `/compliance/` · `/about/` · `/contact/` · `/terms/` · `/imprint/`

| القرار | السبب |
|---|---|
| **صفر إيموجي** — أيقونات SVG داخلية بدلها | الإيموجي بيتغيّر شكله حسب النظام وبيدي إحساس غير احترافي |
| CSS/JS كملفات منفصلة باسم فيه **هاش المحتوى** | ~18 ك.ب أقل على كل صفحة في موقع من 9 صفحات، وكسر الكاش بيحصل تلقائيًا |
| **رندر مسبق** لملفات ثابتة وقت البناء | أباتشي + CDN بيقدّموها ببروتلي من غير ما يشتغل نود أصلًا |
| روابط بشرطة مائلة في الآخر (`/features/`) | بتخلّي أباتشي يقدّم الملف مباشرة من غير تحويل 301 |
| **ممنوع** توليد `.htaccess` في جذر الموقع | الملف الموجود فيه إعدادات Passenger — الكتابة فوقه بتوقّع النظام كله |

**أماكن الصور:** 16 مكان صورة، كل واحد بيعرض إطار فيه اسمه ومقاسه لحد ما الصورة توصل.
البرومبتات كاملة في [`docs/KASSENTA_IMAGE_PROMPTS.md`](docs/KASSENTA_IMAGE_PROMPTS.md) —
ترمي `<slot-id>.webp` في `public/brand/site/` وتظهر لوحدها من غير أي تعديل كود.

#### ج) التقسية الأمنية

| البند | قبل | بعد |
|---|---|---|
| **SEC-04** مطابقة المسارات العامة | `path.startsWith(route)` — `/api/store` كان بيفتح `/api/store-settings` | مطابقة عند حدود المسار ([`server/tenantAuth.ts`](server/tenantAuth.ts)) — 23 حالة اختبار بتعدّي |
| تحديد المعدّل | مفيش | [`server/rateLimit.ts`](server/rateLimit.ts) على دخول السوبر أدمن (10/IP + 5/إيميل) وPIN الموظفين (20/IP + 40/رخصة) ودخول العملاء والتحقق من الرخصة ونموذج التواصل |
| ترويسات الأمان | `Cross-Origin-Opener-Policy` بس | + `nosniff` · `Referrer-Policy` · `X-Frame-Options` · `frame-ancestors` · `Permissions-Policy` · HSTS |
| بادئة مفاتيح الترخيص | `BARMAGLY-` | `KASSENTA-` للمفاتيح الجديدة (القديمة بتشتغل زي ما هي — البحث بالنص الكامل) |

**كسر PIN من 4 أرقام** كان بياخد ثواني قبل التحديد؛ دلوقتي 20 محاولة كل 10 دقايق لكل جهاز.

#### د) الأداء

| البند | النتيجة |
|---|---|
| الضغط | **CDN بتاع Hostinger بيعمل gzip/brotli بالفعل** — إضافة middleware في إكسبريس كانت هتبقى تكرار |
| كاش الأصول | صور المنتجات و`assets` بقوا **30 يوم**، أصول الموقع **سنة + immutable** |
| صفحات الموقع | HTML مرندر مسبقًا يقدّمه أباتشي — نود مش بيشتغل خالص |
| فهارس قاعدة البيانات | **مفيش نقص** — الجداول الكبيرة (`customers` 39.5 ألف صف، `products`، `calls`) عندها فهرس `tenant_id`؛ و`inventory`/`notifications`/`activity_log` مش عندها عمود tenant أصلًا ومفهرسة على `product_id`/`branch_id`/`employee_id` |

#### هـ) الجودة والتحقق

| المقياس | قبل | بعد |
|---|---|---|
| أخطاء TypeScript في الواجهة | 56 | **0** |
| مفاتيح ترجمة ناقصة | 45 (كانت بتظهر كنص خام في الواجهة) | **0** — 731 مفتاح × 3 لغات، بدون تكرار |
| إيموجي في واجهة التطبيق | 103 سطر | **0** |

**اختبار حي بعد النشر:** 10 مسارات في التطبيق × وضعين (فاتح/داكن) برخصة حقيقية ودخول موظف —
**صفر خطأ جافاسكريبت، صفر شاشة خطأ، صفر تجاوز أفقي** على 1440px و834px و390px.

---

### المرحلة 0.6 · التباين المُقاس والصور والتطبيقات — منفّذة (12 أغسطس 2026)

#### أ) فحص التباين — بالقياس مش بالتقدير

[`scripts/audit-contrast.js`](scripts/audit-contrast.js) بيدخل التطبيق برخصة حقيقية،
بيمشي على **10 شاشات × عرضين**، بيجمّع سلسلة الخلفيات عشان يطلع الخلفية الفعلية،
وبيقيس التباين الحقيقي لكل نص وأيقونة.

**لقى 35 زوج ألوان راسب — منهم واحد كنت أنا مؤكّد إنه سليم:**

| الاكتشاف | قبل | بعد |
|---|---|---|
| الأكسنت الفاتح `#0C8F85` | **3.97:1** على الأبيض (التعليق كان مكتوب فيه 4.6:1) و**3.16:1** على تدرّجه الخاص | `#0A6E65` — **6.1:1** و**4.9:1** |
| الحبر فوق الأكسنت (`textDark`) | كحلي غامق في الوضعين ← **3.09:1** على الأكسنت الجديد | أبيض في الوضع الفاتح ← **6.1:1** |
| هيدر الموبايل ويب | خلفية شبه بيضاء + **أيقونات ونص أبيض** ← غير مرئي تمامًا | توكنات surface/text/border |
| 7 ألوان تصنيف مكتوبة يدويًا في 35 موضع | **1.7–2.5:1** على صفحة بيضا | توكنات بنسخة فاتحة مقاسة |
| شاشة البداية | بتاخد توكنات الثيم فوق خلفيتها الكحلية الثابتة | مجموعة ألوان ثابتة على الداكن |

**الدرس:** الخلفية الأسوأ في الوضع الفاتح **مش الأبيض** — هي تدرّج اللون نفسه بنسبة ~13%
اللي بيترسم ورا الشارات وأيقونات الأقسام. اللون اللي بيعدّي على الأبيض ممكن يسقط عليها.

**174 مشكلة ← صفر مشكلة حقيقية.**

#### ب) الصور

16 صورة اتسلّمت واتعالجت بـ[`scripts/optimize-site-images.py`](scripts/optimize-site-images.py):
**28.9 ميجا ← 1.4 ميجا** (أصغر بـ95%).

- أسماء الملفات كانت فيها **مسافات صفرية العرض (U+200B)** — الرابط شكله سليم لكنه مش بيفتح.
- **صفر قص.** المولّد رجّع نسب عرض مختلفة عن البريف، فالتصميم بقى يتبع الصورة بدل ما يقصّ الموضوع.
- `home-devices` طلع بخلفية شفافة فعلًا، فاتحفظت الشفافية والإطار اتشال — بيشتغل في الوضعين.
- بطاقة المشاركة اتركّبت فوق الرندر الجديد وبقت **JPEG 74 ك.ب بدل 526**.

#### ج) SEO

`width`/`height` على كل صورة (منع قفزات التخطيط) · `preload` + `fetchpriority` لصورة الهيرو ·
بيانات منظّمة (SoftwareApplication · Organization بالـUID · WebSite · **FAQPage** · Product بـ3 عروض ·
ItemList · ContactPage · BreadcrumbList) · **خريطة صور** بـ15 مدخل ·
العناوين والأوصاف اتظبطت لتقاس نتيجة البحث (**الـ9 صفحات تحت 60 و158 حرف**).

#### د) فجوة اكتشفتها في النشر

مجلد `delivery-app/` — **متجر العميل وتطبيق السائق** — **مكانش في قايمة ملفات سكريبت النشر أصلًا**.
يعني `kassenta.com/customer/` كان لسه بيقدّم *"Barmagly — Order Food"* طول الوقت.
اتضاف للحزمة واتصلح، والعناوين الحيّة بقت Kassenta.

المسح بيحمي المعرّفات اللي هي **بيانات مش نصوص**: اسم حدث `barmagly-open-cart`،
وبادئة التخزين `barmagly_`، ومعرّفات الحزم في المتجر، ودومين الشركة `barmagly.tech`،
و slug المستأجر التجريبي `barmagly` الموجود كصف في قاعدة البيانات.

#### هـ) بناء التطبيقين

- **Kassenta POS** — `tech.barmagly.pos` · versionCode **9 ← 10**
- **Kassenta Order** — `com.barmagly.customer` · versionCode **3 ← 4** · اسم وأيقونات جديدة
  (كان شايل أيقونة عربة بنفسجية من الهوية القديمة)

معرّفات الحزم والـslugs **ما اتغيّرتش** — دي اللي بتحدّد الإدراج المنشور في المتجر.
البناء في WSL Ubuntu لأن ويندوز مش قادر (MAX_PATH) — التفاصيل في [[wsl-android-build]].

---

### المرحلة 1 · الأساس الآمن — 4 أسابيع

- [x] **توكن موظف + `requireRole` (SEC-01)** — `server/employeeAuth.ts` جديد · جدول صلاحيات مركزي `ROUTE_RULES` · `attachEmployee()` + `guardTenantRoutes()` في `server/index.ts` · العميل بيبعت `x-employee-token` · ⚠️ الوضع `soft` لحد ما كل العملاء يتحدّثوا
- [x] **تشفير PIN (SEC-02)** — bcrypt + هجرة شفافة عند أول دخول ناجح · وقف تسريب الـPIN في رد تسجيل الدخول · منع تطابق PIN عبر مستأجرين مختلفين · **تحديد المحاولات** 20/IP + 40/رخصة كل 10 دقايق ([`server/rateLimit.ts`](server/rateLimit.ts)) — *مكتمل*
- [x] **حذف سر السييد (SEC-03)** — مسارات السييد بترجع 404 في الإنتاج، مفيش سر بيفتحها
- [x] تشديد المسارات العامة لتطابق دقيق (SEC-04) — *منفّذ في المرحلة 0.5*
- [x] **تأمين WebView تطبيق العميل (SEC-05)** — `originWhitelist` محصور في دومين المنصة · الروابط الخارجية بتفتح في متصفح النظام · الدومين من متغير بيئة
- [~] تصفير أخطاء TypeScript (QLT-01) — **130 → 49**، والواجهة (app/components/lib/constants) بقت **صفر**؛ الباقي كله في `server/` من تحويل Drizzle لـMySQL
- [x] **إصلاح BUG-01 و BUG-02** — معرّف سلة بعدّاد بدل `Date.now()` + مفتاح دمج على الهوية · `await` الناقصة لـStripe
- [ ] إعداد CI + Sentry

**معيار الإنجاز:** صفر خطأ أنواع · اختبار اختراق داخلي ناجح · مفيش مسار كتابة بدون تحقق دور.

### المرحلة 2 · الصحّة المالية — 4 أسابيع

- [ ] محرك ضرائب متعدد الفئات + تبديل dine-in/takeaway (TAX-01)
- [x] **تصحيح الضريبة الافتراضية 7.7 → 8.1** (الجزء العاجل من TAX-01)
- [ ] حساب بالسنتات + تقريب 0.05 (FIN-01)
- [ ] دعم الأسعار الشاملة/غير الشاملة للضريبة
- [x] **إزالة رسوم الـ20 المثبتة (BIZ-01)** — بقت `minOrderAmount` في إعدادات المستأجر، معطّلة افتراضيًا، ودليفري فقط
- [ ] تعدد العملات والتنسيق حسب اللغة (I18N-01)
- [ ] تقرير Z للوردية وتسوية الدرج

**معيار الإنجاز:** مجموعة اختبارات على السلة والضرائب · مطابقة يدوية لـ50 فاتورة حقيقية بصفر فرق.

### المرحلة 3 · الأوفلاين والتشغيل — 4 أسابيع

- [ ] وضع أوفلاين كامل ومزامنة idempotent (OPS-01)
- [ ] قفل صفّي للمخزون (`FOR UPDATE`)
- [ ] طباعة ESC/POS + توجيه المطبخ + KDS (OPS-02)
- [ ] تحسين WebSocket بإقرار استلام واحتياطي polling
- [ ] فتح درج النقدية

**معيار الإنجاز:** بيع ساعة كاملة بدون إنترنت ثم مزامنة بصفر فقد أو تكرار.

### المرحلة 4 · التصميم والوايت مود — 4 أسابيع

- [x] نظام الرموز والثيمات + ThemeProvider — *منفّذ في المرحلة 0.5*
- [x] وضع فاتح مصمَّم بتباين AA — *منفّذ في المرحلة 0.5*
- [x] تنظيف الإيموجي من واجهة التطبيق (103 سطر ← 0) — *منفّذ في المرحلة 0.5*
- [x] تصفير مفاتيح الترجمة الناقصة (45 ← 0) — *منفّذ في المرحلة 0.5*
- [ ] وضع عالي التباين (فوق الوضعين الحاليين)
- [ ] تفكيك الشاشات العملاقة لمكوّنات (`index.tsx` لسه ~3,800 سطر)
- [ ] اختصارات لوحة المفاتيح على الويب
- [ ] تدقيق وصول WCAG AA كامل (تركيز لوحة المفاتيح، أدوار ARIA، قارئ الشاشة)
- [ ] إضافة الفرنسية والإيطالية + فحص آلي لتطابق مفاتيح الترجمة في CI

**معيار الإنجاز:** تبديل الوضعين بدون أي لون مكسور · تباين AA على كل شاشة · 5 لغات مكتملة.

### المرحلة 5 · الموديولات والانطلاق — 4 أسابيع

- [ ] سجل الموديولات وبوابات القدرات (سيرفر + واجهة)
- [ ] حزم: مطاعم · كافيهات · سوبرماركت · صيدليات
- [ ] البيع بالوزن (DEBT-06)
- [ ] تكامل TWINT
- [ ] صفحة تسعير الباقات + ربط الاشتراك بفتح الموديولات

**معيار الإنجاز:** تسجيل مستأجر جديد بنشاط مختلف يدي واجهة مناسبة تلقائيًا · أول 3 عملاء دافعين.

### ملخص زمني

| المرحلة | الأسابيع | التركيز |
|---|---|---|
| 1 | 1–4 | أمان |
| 2 | 5–8 | مالية وضرائب |
| 3 | 9–12 | أوفلاين وتشغيل |
| 4 | 13–16 | تصميم ووايت مود |
| 5 | 17–20 | موديولات وانطلاق |

---

## 11. الأسبوع الأول — مهام فورية

| # | المهمة | الملف | الوقت |
|---|---|---|---|
| 1 | حذف القيمة الاحتياطية لسر السييد وتعطيل مسارات السييد في الإنتاج | `server/tenantAuth.ts:109` | 30 دقيقة |
| 2 | إضافة `await` الناقصة لعميل Stripe | `server/routes.ts:4322` | دقيقتان |
| 3 | تعديل الضريبة الافتراضية 7.7 → 8.1 | `lib/cart-context.tsx:58` | دقيقتان |
| 4 | تعطيل رسوم الـ20 فرنك | `lib/cart-context.tsx:121` | ساعة |
| 5 | حصر `originWhitelist` في دومين المنصة | `customer-app/app/index.tsx` | 15 دقيقة |
| 6 | تحديث `replit.md` (MySQL لا PostgreSQL) | `replit.md` | 20 دقيقة |
| 7 | **البدء في توكن الموظف** — أطول بند وأهمه | جديد | أسبوعان |

> البنود 1–6 مجموعها **أقل من ساعتين** وبتشيل أخطر ثغرة تدمير بيانات وأوضح خطأ مالي.

---

## 12. ملحق: مقاييس خط الأساس

مقاسة بتاريخ 9 أغسطس 2026 — استخدمها كنقطة مقارنة بعد كل مرحلة.

| المقياس | القيمة | الهدف |
|---|---|---|
| أخطاء TypeScript | **130** | 0 |
| مسارات بتحقق صلاحيات | **0** | 100% من مسارات الكتابة |
| اختبارات آلية | **0** | تغطية 60%+ على منطق الفلوس |
| مواضع CHF مثبتة | **128** | 0 |
| أكبر ملف واجهة | **4,631** سطر | < 500 |
| أكبر ملف سيرفر | **4,791** سطر | < 500 |
| اللغات المدعومة | **3** (EN/AR/DE) | 5 (+FR/IT) |
| جداول قاعدة البيانات | 66 | — |
| مسارات السوبر أدمن المحمية | 68 / 69 ✅ | 68 / 69 |
| قدرة العمل أوفلاين | **صفر** | ساعة+ |
| فئات الضرائب المدعومة | **1** | متعددة |
| رصد الإنتاج | **غير موجود** | Sentry + سجل منظّم |

---

<div dir="ltr">

**Kassenta POS System** — Technical Audit & Development Roadmap
Generated from direct source-code inspection · 9 August 2026

</div>
