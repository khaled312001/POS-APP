{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npx expo export && node scripts/post-export.js",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
