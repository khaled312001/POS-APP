{
  "name": "caller-id-bridge",
  "version": "1.0.0",
  "description": "Local bridge: reads incoming calls from AVM FRITZ!Card USB via CAPI 2.0 and sends to POS server",
  "main": "capi-bridge.js",
  "scripts": {
    "start": "node capi-bridge.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "koffi": "^2.8.0",
    "node-windows": "^1.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
